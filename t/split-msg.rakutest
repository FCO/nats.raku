#!/usr/bin/env raku

use Test;
use Test::Mock;

use lib 'lib';

use Nats;
use Nats::Message;

use-ok 'Nats';

# TODO: Frame reassembly - requires input buffering in handle-input
# When TCP splits a frame across chunks, the parser needs to buffer
# partial frames and reassemble them before parsing.
{
    skip 'Frame reassembly not yet implemented', 4;
}

done-testing;
